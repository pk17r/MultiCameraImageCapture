# Automatically added by dh_installudev
if [ "$1" = install ] || [ "$1" = upgrade ]; then
	if [ -e "/etc/udev/rules.d/z60_ir-keytable.rules" ]; then
		if [ "`md5sum \"/etc/udev/rules.d/z60_ir-keytable.rules\" | sed -e \"s/ .*//\"`" = \
		     "`dpkg-query -W -f='${Conffiles}' ir-keytable | sed -n -e \"\\\\' /etc/udev/rules.d/z60_ir-keytable.rules '{s/ obsolete$//;s/.* //p}\"`" ]
		then
			rm -f "/etc/udev/rules.d/z60_ir-keytable.rules"
		fi
	fi
fi
# End automatically added section
